{
  "name": "egyptian-exchange-app",
  "version": "1.0.0",
  "description": "Mobile app wrapper for elmezlawyholding exchange website",
  "scripts": {
    "sync": "cap sync",
    "resources": "cap assets generate --ios --android",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "npm run sync android && cd android && ./gradlew assembleRelease",
    "build:ios": "npm run sync ios && xcodebuild -workspace ios/App/App.xcworkspace -scheme App -configuration Release"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2",
    "sharp": "^0.35.4"
  }
}
